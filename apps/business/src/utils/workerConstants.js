// Slugs that belong to the Alex / Chief-of-Staff orchestrator family.
// These are filtered out of places that list domain workers to avoid
// rendering duplicate "Alex" cards alongside regular worker cards.
export const ALEX_SLUGS = new Set(["alex-platform", "alex", "chief-of-staff"]);
