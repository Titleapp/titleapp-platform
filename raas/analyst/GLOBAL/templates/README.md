# Analyst Templates — GLOBAL

Templates define the required structure of Analyst outputs.

Templates are intentionally simple JSON schemas so that:
- GPT-first UI (Door 2) and Web UI (Door 1) produce comparable outputs
- workers can fill sections deterministically
- tenants can overlay/extend without breaking required sections

These are v0 defaults for the Analyst vertical.
