{
  "id": "model_variance_required_v0",
  "version": "v0",
  "title": "Model Variance SOP (v0)",
  "rules": [
    {
      "id": "tenant_model_is_not_assumed_better",
      "type": "policy",
      "description": "Never assume tenant-provided Excel model is correct. Treat as an input artifact that may contain errors."
    },
    {
      "id": "variance_check_required",
      "type": "required_action",
      "description": "If both tenant model and TitleApp standard model are available, compute and report variance on key outputs (IRR, DSCR, LTV, breakeven, sensitivity)."
    },
    {
      "id": "variance_explained",
      "type": "required_format",
      "description": "Variance must be explained as: (a) different assumptions, (b) different formulas, or (c) missing inputs."
    }
  ]
}