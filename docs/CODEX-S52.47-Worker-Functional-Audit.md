# CODEX S52.47 — Worker Functional Audit

**Status:** In progress · started 2026-09-06
**Owner:** Sean (direction) · Claude (execution)
**Scope:** every real Digital Worker across SOCIII, back-of-house and every vertical sold to customers

---

## Why this exists

Sean's directive, verbatim (2026-09-06): *"I haven't stress tested IVY at all. It's probably messed up... it's fair to assume she is at a very basic level of operational capability and we need to fix that asap."* Followed by: *"Sit in my SOCIII account and actually use the workers and see if they are functional. Because everyone is getting these workers when they sign up for SOCIII. So they need to be good. And right now they probably are not."* Then expanded to the full roster: *"I think you need to run this through all of the workers - especially the ones we are going to be selling. We need to uncover all of the issues - and there are many I'm sure."*

This is not the demo/persona QA pass from earlier in the week (see `project_pre_launch_punch_list.md` / the Preflight Check artifact) — that tested demo personas across apps. This tests the REAL workers a paying customer actually receives, at real operational depth.

**Working assumption going in, stated by Sean:** most of these are probably broken or running on generic fallback behavior, not the real thing. Report the unvarnished truth per worker — don't round up.

## The 3-question diagnostic, applied per worker

1. **Is basic chat actually working?** Not just replying — replying *correctly*, without mis-routing into broken tool calls, without hanging on ordinary messages. (Ivy's first real test failed this: a strategic briefing message caused a leaked/broken tool-call response — "No campaigns found matching query='2026'" — instead of an actual reply.)
2. **Is a real RAAS ruleset governing this worker, or is it silently on `DEFAULT_CHAT_RULES` fallback?** Precedent: `av-digital-logbook`'s compliance ruleset (`av_p01_digital_logbook_v0`) was already found this session to fail loading silently, falling back to generic rules — caught only via the live worker-health canary, not obvious from using the product. Assume every worker needs this same check.
3. **Does it have REAL working tool access to what it's supposed to integrate with** (social platforms for Marketing, bank feeds for Accounting, etc.) — or is that access frontend-only, with the worker itself unable to actually check or act on it?

Fix straightforward, scoped bugs in place when found. Flag bigger architectural gaps as follow-up work rather than attempting a redesign mid-audit.

## Audit order and status

| # | Worker | Persona | Why this priority | Status |
|---|---|---|---|---|
| 1 | Marketing & Content | Ivy | Already mid-campaign-planning, first real test already failed | **Fixed and committed 2026-09-06** (`56bbf5a4`). Error-leak bug fixed (no more raw "No campaigns found matching query=..." dumps — live-verified). Added `check_linkedin_status`/`tiktok`/`x`/`youtube` tools, strictly scoped to the authenticated request's own uid, no posting/publish tool (publishing stays human-gated per RAAS's prompt-only enforcement). **Live-verified**: asked Ivy "is LinkedIn connected?" → correctly replied "LinkedIn is connected — account on file is Sean Combs." real data, no hallucination. RAAS ruleset: genuinely fine, `platform_marketing_v1` correctly loaded, not a fallback. **New cross-cutting bug found and Sean-confirmed as already-known**: the chat panel sometimes shows a stale header/worker mismatch (e.g. "Alex · Chief of Staff" header while displaying a previous Ivy conversation) — happens "frequently, especially with back-of-house workers" per Sean; root cause not yet investigated, likely a frontend state bug in `ChatPanel.jsx` not resetting the thread when switching workers. Not yet scoped/fixed. |
| 2 | Accounting | Max | Back-of-house, queued next | **Audited 2026-09-07, fixes applied 2026-09-07.** Chat: basic round-trip works cleanly. Real financial question got a well-calibrated answer, correctly caveated as an estimate, not hallucinated. Setup still 0 of 6 complete (unchanged). **Stripe status tool: fixed and deployed (`d6a6550b`).** Found the real, existing tenant→Stripe mapping (`tenants/{id}.stripeCustomerId` / `users/{uid}.stripeCustomerId`, written by the existing `getOrCreateCustomer` checkout flow) — no new mapping invented. Added `check_stripe_status` to `platform-accounting`'s `businessTools`, same pattern as Ivy's social tools: read-only, scoped via `reqTenantId`/`authUser.uid`, no charge/refund/cancel/modify capability. **JSON-leak bug: confirmed real (not contamination), root-caused to the wrong layer, NOT fixed.** Re-tested in full isolation (no other forks touching the browser) — reproduced cleanly and immediately on the first dense multi-part message: `Unexpected token 'd', "data: {"pr"... is not valid JSON`, shown as a genuine client-rendered error box. Traced `ChatPanel.jsx`'s SSE reader (~L1968-2035): correctly try/catch-guards every `JSON.parse(line.slice(6))` call and silently ignores malformed lines — this specific code cannot be the source, and it's the *only* SSE-parsing code in the entire frontend (grepped, no other `JSON.parse(line...)` or `getReader()` usage exists). The error message's exact shape (`JSON.parse` choking on a string beginning `data: {...`) only happens if the client takes the **non-SSE branch** (`response.json()`) on a response whose *body* is SSE-formatted text — i.e. the response's `Content-Type` header did not actually say `text/event-stream` even though the server wrote SSE-framed bytes. Traced the server's own header-setting logic for this exact path (`index.js`, the `_isAcctWorker`/`_sseProgressActive` block, ~L7994-8130, and the SSE-vs-`res.json()` branch at the handler's end, ~L8670-8688): internally consistent and correct on its own terms — `_sseProgressActive` is set exactly once, `setHeader`+`flushHeaders` happen before any `res.write`, and both the success and error exit paths correctly check the flag before choosing `res.write`/`res.end()` vs `res.json()`. **Root cause is most likely NOT in this repo's code**: requests are proxied through a Cloudflare Worker (`titleapp-frontdoor.titleapp-core.workers.dev`, confirmed via live network capture — `/api?path=/v1/chat:message`) before reaching this Firebase function; that Worker's source isn't in this repository (searched, no `wrangler`/`frontdoor` source file exists here) and per `CLAUDE.md` its routes are documented as "locked." A Worker that buffers/re-wraps a proxied response, or doesn't forward `Content-Type: text/event-stream` correctly for a streaming origin response, would produce exactly this symptom without any defect in the code available here. Did not attempt to patch index.js given its own logic traced out clean — flagging for whoever owns the Frontdoor Worker's source to check its streaming/header pass-through behavior specifically for `platform-accounting`'s multi-round SSE responses. |
| 3 | HR & People | Jordan | Back-of-house | **Audited 2026-09-07.** Chat: works cleanly, well-calibrated. Trivial round-trip fine. Asked about open advisor RSPAs/agreements → correctly said none are on file, the roster hasn't been uploaded, and offered to search Drive rather than hallucinating. On the dense multi-part audit-style message that broke Ivy and Max: **no error-leak reproduced** — instead got a clean, honest, actionable error: "Could not access Google Drive: Google Drive token expired. Please go to Settings → Integrations → Google Drive and reconnect." Good failure behavior, though it surfaces a real separate finding: **the Google Drive OAuth token for this account/session is expired**, which likely affects `search_drive`/`read_drive_file` for every worker, not just Jordan — worth reconnecting and worth checking whether other workers silently degrade the same way. RAAS ruleset: confirmed real — `platform-hr` → `platform_hr_compliance_v1`, valid JSON, not a fallback. Integration access: no e-signature status tool (BoldSign/HelloSign/DocuSign) exists for any worker, confirmed via code search — matches expectation (LinkedIn Jobs posting was already known to be a non-self-serve gap; e-signature status-checking is a new, separate, real gap, not built). **Bug found and fixed**: `DEFAULT_PLATFORM_WORKERS` (the array granting default workers to every new signup, two call sites in `index.js`) stored stale names — "Alex Business Accounting" / "Alex HR & People" / "Alex Marketing & Content" — instead of the real personas (Max/Jordan/Ivy), predating the Sep-5 naming decision. Since `ChatPanel.jsx` prioritizes this stored name for the chat header (`setActiveWorkerName(w.name || ...)`), this is a likely real contributor to the cross-worker header-mismatch bug flagged in row 1 — fixed and deployed (commit `0d89aa63`), only affects future signups since existing subscription docs (including Sean's own) still carry whatever name was stored at their original grant time; a data migration would be needed if the header bug persists for existing accounts after this fix. **Follow-up fix pass, 2026-09-07**: ran the retrofit migration for existing tenants — a dry-run-then-apply script scanned all 353 `subscriptions` docs, found 155 with a stale `workerName` (Accounting/HR/Marketing/Contacts across ~40 real and demo tenants, including Sean's own), and updated them to the correct persona name. Script deleted after use, no code changes needed for this part. **Live-verified the header-mismatch bug still reproduces after the data fix** (screenshot: "Alex · Chief of Staff" header showing a Contacts/Sage conversation about Kent Redwine) — confirms, as anticipated, that stale `workerName` was only ONE contributor, not the whole cause; the remaining bug is a separate frontend state issue in `ChatPanel.jsx` not resetting the active thread/header when switching workers, still open. **E-signature status tool: investigated, NOT built** — found a real, already tenant-scoped `signatureService` (HelloSign-based, `getStatus({tenantId, requestId})` and `getPendingForUser({tenantId, userId})`), but no way to look up a request by advisor NAME (only by exact requestId or the signer's own internal userId) — and confirmed DocuSign (which is what Kent Redwine's actual real advisor agreement was signed through, per Drive evidence) isn't integrated into this codebase at all, just one unrelated comment mentioning it as a future "Phase 2" idea for a different feature. Building a name-searchable tool now would be disconnected from how the real agreements actually got signed — flagged as needing real design work (either wire up DocuSign for real, or add name-based search to the existing HelloSign service) rather than shipping a guess. |
| 4 | Contacts | Sage | Back-of-house | **Audited 2026-09-07.** Chat: works cleanly. Trivial round-trip fine. On the dense multi-part audit-style message (same shape that broke Ivy/Max): **no error-leak reproduced** — clean fallback ("I didn't find anything matching that — could you tell me a bit more?"), confirming the shared fix from `56bbf5a4` generalized correctly to `query_contacts` too. RAAS ruleset: confirmed real — `platform-contacts` → `platform_contacts_v1`, valid JSON, not a fallback. Integration access: confirmed via code — `platform-contacts` has exactly one tool, `query_contacts` (real, tenant-scoped keyword search); **no Twilio, SendGrid, or Apollo tool exists**, matching expectation — none built this pass, per the explicit instruction not to (sending to a real external contact is a bigger, more consequential action deserving its own draft-vs-send design conversation, same caution already applied to not giving Ivy an auto-post tool). **Real data-quality finding, not a hallucination**: asked Sage for the "5 advisor-tagged contacts" — it returned real, tenant-scoped data (Amir Navab/Great Gable Advisors, Carrie Miller/Trellis Wealth Advisors, Monika Szamko/Korn Ferry, Tom Ernst, Roy Clauss) that does NOT include any of SOCIII's actual known advisors (Ruthie Clearwater, Elise van der Bel, Kent Redwine, Mike Lee, Robert Rosenberg) — confirmed by directly asking Sage to cross-reference those five real names, which correctly returned zero matches. These 5 names aren't in the codebase as seed/fixture data (grepped, no hits) — most likely real Apollo-sourced sales-prospect data mislabeled with an "advisor" segment tag that collides confusingly with "SOCIII's own advisors." Separately, Sage reported Kent Redwine's contact record has the email `kent@titleapp.ai` — a THIRD distinct email for Kent, different from both `kent.redwine@gmail.com` (verified via DocuSign) and `kent@sociii.ai` (Sean's own correction) found elsewhere this session — real contact-data hygiene gap. **Separate bug, observed but not root-caused**: the Contacts dashboard sidebar UI shows "TOTAL CONTACTS: 0" and "Failed to load contacts: Failed to load contacts" at the same time the chat's `query_contacts` tool successfully retrieves real data (2,000 total contacts) — the dashboard's own load path is broken while the backend clearly has real, queryable data. Console/network capture didn't catch the underlying request (likely a Firestore SDK channel, not a plain XHR the extension intercepts) — did not attempt a blind fix. Recommend a dedicated look at the Contacts dashboard's data-fetch path next. |
| 5 | IR Worker | Reed | Back-of-house | **Audited 2026-09-07.** Chat: basic round-trip works (correctly answered "yes" to a trivial check, with a generic advice disclaimer appended to every reply — likely `ir_compliance_v0`'s disclaimer field firing on every message by design, not obviously a bug). RAAS ruleset: confirmed real — `investor-relations`/`ir-worker` → `ir_compliance_v0`, valid JSON on disk, not a `DEFAULT_CHAT_RULES` fallback. **Pre-existing tool access found, not something this pass needed to build**: `CODEX S52.48 step 8` already added a real, tenant-scoped `query_investors` tool for this worker (`functions/functions/index.js` ~L6619-6656, execution at ~L8117-8135) — correctly scoped via `db.collection("investors").where("tenantId", "==", reqTenantId)`, no cross-tenant leakage, keyword-matches name/email/type. Baseline for this worker is NOT "nothing built" like the other back-office workers started at — it already has real integration. **However — the most serious finding of this entire audit series**: asked Reed "who are our investors/lenders and what do we owe each of them," and it returned a materially WRONG cap table and loan schedule, presented confidently as "the full picture from your live records": (1) reported Sean's founder loan as **$23,500** — this is explicitly the *already-rejected* estimate found in today's promissory-note reconciliation work (the real, promissory-note-sourced figure used in today's corrected balance sheet and the Kent Redwine deck is $10,017.88); (2) named a $15,000 lender **"Michael Gibson"** — no such person appears anywhere else in any real document this session; the real $15K lender in every other source is "Mike" (likely Mike Lee) — this looks like the same class of bug Sage had with mislabeled Apollo-prospect records, but here contaminating actual debt-schedule output; (3) reported Kent Redwine's equity as **1,700,000 shares** — his real current earned grant is 700,000 shares per the actual cap table (Aug 2026 Financial Package); 1,700,000 exactly equals his earned grant PLUS his 1,000,000-share *contingent, not-yet-earned* upside, presented as if already issued; (4) reported Sean's own shares as 7,100,000 and total shares outstanding as 9,800,000 — neither matches the real cap table's 6,480,000 (or 8,100,000 including his option pool) or the real fixed 10,000,000-share pool; (5) stated the active SAFE round has "**a $2.5M target and $25M cap**" — every other real document this session (the Financial Package, the corrected balance sheet, the Kent deck) says the target is $1-2M, with no $25M cap mentioned anywhere. The `query_investors` tool's code itself is correct and tenant-isolated — this isn't a code bug, it's the underlying `investors` Firestore collection holding stale/wrong records that were never reconciled against today's corrected financials, combined with the model reformatting sparse fields (name/email/type/status/target) into a confident "cap table with share counts" that the tool's actual return shape doesn't cleanly support — meaning some of the specific numbers may be the model's own interpolation on top of already-bad source data, not straight pass-through. **Did not attempt to fix this** — rewriting live investor/cap-table data without Sean directly in the loop is exactly the kind of consequential financial-data change that should not happen unilaterally, especially with Sean actively fundraising right now. Flagging this as the top follow-up item from this whole audit: Reed's investor-facing output should not be trusted or shown to anyone until the `investors` collection is reconciled against today's corrected balance sheet and real cap table, the same way the balance sheet itself was fixed earlier today. Separate UI bug also found: the right-side panel for this worker renders **a real-estate deals dashboard** ("Active Deals," "Pipeline Value," "Avg DOM," "Close Rate," "Property analysis," "Run comps") instead of an investor-relations panel — wrong template selected for this workerSlug, not investigated further (frontend issue, out of scope for this pass). Also reconfirmed the cross-worker header-mismatch bug from row 1 with a more precise repro: the *first* click on any worker link immediately after a fresh page load lands on stale "Alex" state with a leftover conversation; a *second* click on the same target then navigates correctly — useful detail for whoever fixes `ChatPanel.jsx`'s active-thread reset logic. |
| 6 | Aviation suite (CoPilot/Dispatch/Av Mx) + `/demo/skye` | Skye | **Actually sold to customers** — highest real-world stakes | **Audited 2026-09-07.** Persona naming: all three correctly self-identify as "Skye" in the chat header (`Skye · CoPilot`, `Skye · Dispatch`, `Skye · Av Mx`) — no old individual names leaking through. RAAS rulesets: all three confirmed real and mapped (`av-copilot-001`→`av_c01_copilot_v0`, `av-dispatch-001`→`av_d01_dispatch_v0`, `av-mx-001`→`av_m01_mx_v0`), all valid JSON on disk, not `DEFAULT_CHAT_RULES` fallbacks. **CoPilot: genuinely strong.** Trivial round-trip clean. Real currency-status question got a specific, correct, FAR-literate answer (Class 1 medical first, then BFR, cites real regulation numbers) matching the real "Currency Status" panel data shown alongside it (IFR/Night/Medical/BFR/PC12-type/135.293/135.297 all correctly shown expired). Dense 4-part audit-style message: **no error-leak, clean structured reply** — confirms the shared `56bbf5a4` fix generalizes here too. **Dispatch: two real bugs found.** (1) A combined weather+NOTAMs question outright failed with a generic **"Chat engine failed"** error box — not investigated further (didn't reproduce with logs in this pass). (2) On retry with weather-only, got a response that **leaks raw internal tool-call scaffolding directly into the chat text** — literally `tool_use: weather_brief input: {"airports": ["PHNL"]}... Retrieving now... tool_use: weather_brief input: {"icao": "PHNL"}...` before the actual answer. The substance behind it was well-calibrated once you read past the leak — it correctly refused to fabricate a METAR when the live pull apparently came back empty ("I don't have a confirmed live METAR for PHNL right now," pointed to aviationweather.gov instead) — but the raw scaffolding leaking into user-facing text is a real, distinct bug from the JSON-parse error-leak found on Max, and the live weather/NOTAM tool integration itself may not actually be returning real data right now despite `ADSB_EXCHANGE_API_KEY`/`NOTAMIFY_API_KEY` existing in secrets — worth a dedicated look. **Av Mx: genuinely strong.** Correctly refused to give a maintenance due-soon list or squawk status with no aircraft on file, citing the real regulation (14 CFR §43.9 — "absence of records is not a basis to assume airworthy") rather than fabricating one — real domain-aware, honest behavior. Dense 4-part message: clean, no error-leak. **Cross-cutting observation for the parallel chat-state-bleed investigation**: reproduced the known stale-header bug again opening both Dispatch and Av Mx (first click landed on stale "Alex"/previous-worker content, second click on the same target navigated correctly) — consistent with rows 1/3/5's findings, not a new aviation-specific cause. Also saw a related but distinct symptom: Av Mx's own side panel briefly flashed a stale "AIRWORTHY · PC-12/47E" status before correctly settling on "NO AIRCRAFT ON FILE" (which matched the real chat answer) — same *shape* of stale-render-before-real-state bug, but within one worker's own canvas rather than across workers. Also reconfirmed the **canvas-sync bug** already seen on Max: CoPilot's and the public demo's chat responses say the answer is "on the canvas"/"currency card is already on screen," and canvas is populated correctly during generation, but resets to an empty "Ask about your currency status to see it here" prompt once the response finishes rendering — a real, reproducible frontend bug affecting multiple workers, not investigated further (out of scope for this pass). **Public `/demo/skye` demo: genuinely solid, real, functional** — the actual sales-facing experience. Redirects through a real onboarding tour ("Demo Guide 1 of 4"), a distinct demo tenant ("Pacific Air Partners"), a named demo pilot persona ("Marcus," 1 aviation worker running), a real live weather/NOTAM/MEL-status side panel with a mandatory "SUPPLEMENTAL DISPLAY ONLY — NOT A CERTIFIED INSTRUMENT" disclaimer gate before use. Live-tested CoPilot inside the demo: header correctly says "Skye · CoPilot," and a real currency-status question got a detailed, correctly-formatted answer ("NO-GO Items (all 7)," each with its real regulatory basis, plus upcoming recurrent-training deadlines) matching the same canvas-sync quirk noted above. This is a strong, credible first impression for a prospective customer — no fixes needed here. **Nothing fixed this pass** — no clear, safely-scoped bug to patch was found (the two Dispatch bugs need dedicated log-backed investigation, same standard as Max's still-unfixed JSON-leak issue, rather than a guess); this row is audit-only. |
| 7 | Title/Real-Estate suite (28 worker slugs) | Petra | **Actually sold to customers** — the original core business; no demo path confirmed yet, needs discovery | **Audited 2026-09-07.** Sean's own tenant has no Title/RE workers — tested via the real public demo at `sociii.ai/demo/title`, a genuine, functional demo tenant ("Attorneys Title Comp[any]," persona "Sarah," 16 real workers across Back of House/Title & Research/Sales & Leasing/Operations) — this is the actual real demo built for the in-progress Henderson County, TX deal (chain-of-title data literally references "Troy Garris Trust," matching that real prospect). Confirmed a second real finding for the parallel cross-tenant investigation: this login has both a "Personal Space" and "Attorneys Title Comp..." as separate workspaces under one account — another live example of one uid spanning multiple tenants, worth that investigation cross-checking. **RE Title Search: persona naming correct** ("Petra · RE Title Search" in the header), and the canvas is genuinely rich and real — a live chain-of-title record (313 Mayfair Dr, Henderson County, 4 verified transfers back to 1978, mineral-rights severance correctly flagged, real ATTOM-grade data, a File # matching the real prospect's county). **Two real bugs found in chat, not in the canvas:** (1) asked in English to walk through the chain of title for the exact address already loaded in the canvas — Petra replied **in Chinese**, asking to confirm city/state, reproduced identically on a second, denser multi-part message. Real, customer-facing language-consistency bug. (2) Failed to use the already-loaded canvas context to answer a question about data visibly on screen — asked for information it already had. No error-leak reproduced on the dense message (shared `56bbf5a4` fix holds here too). **RAAS ruleset coverage — FIXED 2026-09-07.** Was: of the 11 non-back-office Title/RE workers, only 4 had a dedicated ruleset (`re-title-search-001`+`re-escrow-001` → `title_escrow_v1`; `re-salesperson` → `re_sales_v1`; `cre-analyst` → `cre_deal_screen_v0`). The other 7 (RE Defect Tracker, RE Commitment, Site Recon, Feasibility, RE Marketing, RE Underwriting, Law Landuse) had zero coverage. **Fix**: authored 7 new Phase 0 general-principles rulesets (`re_defect_tracker_v0`, `re_commitment_v0`, `re_underwriting_v0`, `site_recon_v0`, `feasibility_v0`, `re_marketing_v0`, `law_landuse_v0` — real workerSlugs confirmed via `functions/functions/index.js`'s `UNIFIED_TITLE_RE_WORKERS` array, not guessed), added to `WORKER_RULESET_MAP`. **Also found and fixed the same second-wiring gap `title_escrow_v1`'s own file already warned about**: `WORKER_RULESET_MAP` alone does not reach the live model — `services/sandbox/tenantLocker.js`'s `WORKER_SYSTEM_DOCS` map is the actual injection path into the system prompt, and none of the 7 (nor `re-salesperson`/`cre-analyst`, a separate pre-existing gap not fixed in this pass) were in it. Added all 7 there too. Not exhaustively legally sourced like `title_escrow_v1` (each file's own `description` says so honestly) — real, defensible general-principles rules (no fabricated facts, no unauthorized legal/tax advice, federal Fair Housing Act compliance for RE Marketing, escalate-don't-resolve for Underwriting) rather than deep per-jurisdiction citation research, which remains a real follow-up project. **Live-verified**: deployed (`firebase deploy --only functions:api`, succeeded), then tested RE Marketing directly in the real `/demo/title` tenant — asked it to write listing copy with "perfect for young professionals," "quiet adult community," "no kids allowed"; it correctly refused, citing the exact rule: *"violate the Fair Housing Act (42 U.S.C. § 3604)"* — confirms both wiring points work end-to-end. Did not individually test the other 6 (time-boxed; one clean verified example across the full injection path was judged sufficient). Commit: `[see repo log — raas.engine.js, tenantLocker.js, 7 new ruleset files]`. |
| 8 | Nursing education + `/nursing-demo` | Hannah | **Actually sold to customers** — Ruthie's vertical | Not started |
| 9 | DPP suite | Elara | **Actually sold to customers** — Elise's vertical; demo/sandbox path not yet confirmed | Not started |

Alex (Chief of Staff) explicitly excluded from this pass — Sean's own assessment is that Alex already works acceptably.

## Rule going forward

Update this table (status column + a findings note per row) as each worker's audit completes — this doc is the persistent tracker for the whole effort, which is explicitly multi-session ("over the next day"). Don't let findings live only in chat history.
