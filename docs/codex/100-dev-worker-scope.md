# CODEX 100 — Dev: the Back-of-House IT/Ops Worker

**Status, updated 2026-09-22 (later same night):** Dev is now built and deployed — `functions/functions/monitoring/devWorker.js`, scheduled every 30 min, plus an on-demand admin route. v1 checks #1/#2/#3/#5 are real and verified live against production Firestore; #7 (watching Dev itself) lives in `workerCanary.js`. One hard requirement from this doc is explicitly NOT yet satisfied: Dev's dedicated viewer-only service account exists but its IAM roles haven't been granted (see Changelog) — Dev currently runs under the default service account. Do not treat Dev as fully shipped per this doc's own access-boundary section until that's closed. The code-level activation gate this doc's finding motivated (`config/capabilityGates.js`, gating `watchMailbox()` in `services/social/gmail.js`) is separately built, deployed, and verified live — see the Changelog for what's real as of which pass.

**Sean, 2026-09-22**, pasting the full "Alex Staff Meeting, IT Worker & Autonomy Levels" concept: proposed a new worker, Dev, that "audits account settings and integrations," starting read-only (diagnose + propose, human approves any change). This doc scopes just that piece — the staff-meeting/ledger/permission-level machinery around it is [[project_worker_team_staff_meeting_dev]] / the pasted CODEX itself, not re-specified here.

This doc exists because scoping Dev tonight immediately surfaced a real, live example of exactly the failure mode Dev is meant to catch — see "The finding that grounds this doc," below. That's not a coincidence worth burying; it's the best possible proof this worker is needed now, not later.

---

## The finding that grounds this doc

While scoping Dev, re-read `docs/codex/97-worker-team-architecture-robustness-comms-and-reporting.md` for the first time since it was written — the doc that, the *previous* night, already decided the persona-email-alias approach and specified a red-teamed gating sequence for going live with it (rate limiter, code-enforced AI-disclosure footer, correction-protocol auto-suspend, task-canary-passing, in that order, before an inbound listener goes live).

The Gmail persona-reply pipeline built the same night as this doc (webhook, moderation gate, send/approve flow) was built **without re-reading CODEX 97 first**, and skips past that gating sequence — none of the four safeguards exist yet. Nothing went live (the activation step, `gmail:startPersonaWatch`, was never called), so no actual harm occurred — but the code was fully built and one function call away from running live, autonomously, in production, in a state its own prior design doc had explicitly red-teamed and gated against. See [[project_gmail_persona_reply_pipeline]] for the full detail; flagged and corrected in memory the same session this was found.

**This is precisely a "config/process drift" and "permission drift" case from Sean's own CODEX** — a capability operating (or about to operate) outside the tier its own governing spec assigned it. A human (me, that night) missed it because the relevant context was sitting in a doc, not something re-checked before building.

**Red-team correction (2026-09-22, same session):** the first draft of this doc put that check inside Dev — which only runs *after* activation, on Dev's next scheduled pass. That's detection, not prevention: between an activation and Dev's next run, a bad activation would already be running live and unmoderated, which is exactly the harm CODEX 97 red-teamed against. **Fixed the same session, before this doc was even finalized:** the gate now lives in code, inside the activation function itself (`watchMailbox()` in `services/social/gmail.js`), via a new machine-readable registry (`config/capabilityGates.js`) — `assertGatesPass('persona-email-inbound-listener')` runs first and throws if any registered gate doesn't pass. **The red-team review also caught that the Cloud Scheduler renewal job was a second, ungated activation path calling the same underlying function** — confirmed true, the job was paused immediately and the gate placed in the one shared function both paths call, closing both at once. Verified live: both `/v1/admin/gmail/renew-watch` and the `gmail:startPersonaWatch` route now fail closed with a clear error listing every unmet gate.

**Dev's role on this specific check is now the backup, not the primary control** — verifying the code-level gate/registry itself hasn't been bypassed, removed, or silently made to pass, not the thing standing between "gate documented" and "gate enforced." That distinction generalizes: **any future gated capability must have its gate implemented in `capabilityGates.js` and asserted inside the activation function itself**, with a CI/lint rule (not yet built) that fails the build if a new gated-capability activation path doesn't reference a registered gate — so forgetting is caught at deploy time, not discovered later by Dev or, worse, not discovered at all.

---

## What already exists — build on it, don't replace it

`functions/functions/monitoring/workerCanary.js` (+ `chatCanary.js`, `qualityCanary.js`) is real, live, already-running synthetic health-check infrastructure:
- A single findings shape: `{ scope, id, severity ("red"|"warn"), reason }`.
- One health doc (`HEALTH_DOC`) holding current status + reds + warns, one `workerHealthEvents` history collection.
- De-duplicated alerting: fires on a *new* red, re-pings if still red after 12h, announces recovery — not a fresh alert on every run.
- Existing checks: rules-loading health, worker-catalog/canvas-spec health.

**Dev is not a new monitoring system. Dev is this system, extended with new checks and a worker identity/voice on top of it**, so it can take a turn in the staff meeting and own ledger commitments like Sean's CODEX describes, instead of just being a silent cron job.

## Dev's v1 checks (concrete, ordered by how immediately valuable they are)

Each check below has a **defined expected state**, not just a pass/fail — per red-team feedback, "no watch because not activated" (fine), "watch expired" (bad), and "watch exists but shouldn't per the gate registry" (red, and the more serious case) must be distinguishable, or they collapse into the same undifferentiated signal.

1. **Gate-registry integrity check** (revised from the original "gated-capability activation" idea, which has now moved into code — see above). Dev's job here is narrower and mechanical: confirm `capabilityGates.js`'s registered predicates still exist, still return real fail-closed results (not a stubbed `true`), and that the live Gmail watch state matches what the registry currently allows. Deterministic code against a deterministic registry — no LLM judgment involved, per the point below.
2. **Gmail watch health.** `gmail.users.watch()`'s returned `expiration` — checks it's still comfortably in the future *when a watch is expected to exist per the gate registry*, distinct from "no watch, correctly, because gates aren't met" (not a finding) and "watch exists despite gates failing" (red — the registry was bypassed). Cross-check: Pub/Sub subscription `gmail-persona-email-push-sub` for a growing undelivered/dead-letter count.
3. **Pending moderation-queue age.** Any `pendingPersonaEmailApprovals` doc older than a threshold (24h) unresolved — warn; red past 48h. **Open design decision, per red-team point on approval-queue single-point-of-failure:** Sean flying LFN 14-on blocks means a 2am red alert may sit unactioned for a full rotation. Needs either a backup approver or a fail-safe default (a stale draft **expires unsent**, never auto-sends on timeout) decided before this pipeline carries real external sends — not an implementation detail to leave implicit.
4. **Config/dependency existence — moved to deploy-time validation, not a Dev check.** Per red-team feedback, a hand-maintained list (like `CROSSMINT_COLLECTION_ID_BASE`) goes stale by construction. The real fix is the service validating its own required config at startup and failing fast; Dev's role shrinks to *reporting* what that startup validation already caught, not independently re-deriving the list.
5. **Existing worker-canary findings, surfaced through Dev.** Not new checks — visibility into `workerCanary.js`'s current red/warn state in one place. **Alert-path consolidation needed:** this and Dev's own findings must share one de-duplication layer with the existing SMS/email `sendAlerts`, or the same incident pages Sean twice.
6. **Permission drift** — deferred, depends on the permission-enforcement build per CODEX 97.
7. **Watching Dev itself.** `workerCanary.js` gets a new check: alert if Dev hasn't reported within its expected interval. Dev cannot be the only thing checking Dev's own health — silence must not read as green.

## Access Dev needs, and the boundary it doesn't cross

**"Read-only" must be an IAM fact, not a convention — red-team correction.** If Dev runs under the default Cloud Function service account (as everything else in this deployment currently does), it likely has Editor and the Admin SDK bypasses Firestore security rules regardless — meaning "Dev is read-only" would be true by agreement only, not enforced. **Hard requirement before Dev ships, not a nice-to-have:** Dev runs under its own dedicated service account, granted only `roles/datastore.viewer`, `roles/logging.viewer`, `roles/monitoring.viewer`, and Pub/Sub viewer — nothing else. This is the thing that makes the "read-only" claim actually true.

- **Firestore, metadata only — not message bodies.** `digitalWorkers`, integration-status docs, `pendingPersonaEmailApprovals`, `personaEmailReplyLog`, `workerHealthEvents`, DTC/chain-anchor docs — but counts, ages, statuses, and timestamps, not email/document content. Per red-team point: these collections can hold real student records or borrower financial data across verticals, and Dev has no need to read message bodies for any check specified above. If a future check genuinely needs content, that's a specific, justified exception, not the default.
- **GCP, read-only:** Cloud Logging Viewer, Cloud Monitoring viewer, Pub/Sub viewer. **Billing note:** cost-anomaly detection is not real-time unless billing export to BigQuery is set up — plan for roughly a day of lag, don't design a check that assumes same-day cost data.
- **Explicitly not:** Google Workspace admin access of any kind, ability to modify OAuth tokens, ability to change any account setting or capability tier directly, its own broad service account. Per Sean's own CODEX ("Dev starts read-only... a human approves any change").

## Interface for v1 — don't wait on the shared-meeting infrastructure

The hardest piece of Sean's full staff-meeting concept (a live, shared, turn-taking multi-worker session) doesn't exist yet and is its own build. Dev's v1 value shouldn't be blocked on it:
- Findings write to a `devFindings` collection, same shape as `workerCanary.js`'s findings (`scope`, `id`, `severity`, `reason`, plus `proposedFix` per Sean's "diagnoses and proposes fixes").
- Real-time notification for anything red reuses the exact SendGrid-notify pattern already built for the moderation-gate approval emails the same night — no new send mechanism needed.
- Once the shared-meeting surface exists, Dev's "turn" reads from the same collection instead of a separate report — this interface is meeting-agnostic on purpose.

## Registration as a real worker

Per the existing `digitalWorkers` Firestore + `useWorkerCatalog.js` normalization pattern, Dev needs a real catalog entry (`display_name`, `capabilitySummary`, a `canvasSpec` if it should render in the UI like other workers) so it isn't a headless cron job pretending to be a persona — it should show up the same way Ivy/Max/Jordan do. Persona email identity (a `dev@sociii.ai` alias) is not urgent for v1 — Dev's primary interface is findings + the eventual meeting, not an inbox — but worth reserving in `personaEmailIdentities.js` for consistency once it's built.

## Dev is deterministic code narrating findings — not an LLM judging them

**Red-team correction, important:** Sean's original CODEX described Dev as both a cron-job extension and a "worker persona with a voice that proposes fixes." Those need to be split cleanly. If an LLM generates `proposedFix` freely from log/queue contents, and those contents can include inbound email text (untrusted, per CODEX 97's own risk register #2), that's a prompt-injection path aimed straight at Sean's own approval step — the exact class of risk this whole doc exists to prevent, reintroduced through the back door. **Every check is deterministic code with a defined pass/fail condition.** A persona layer may narrate findings in Dev's voice for the eventual staff meeting, but proposed fixes are templated per check type, selected from a fixed set, never freely generated from data content.

## Explicit non-goals for v1

- No write access to anything, and no broad service account — its own viewer-only identity, not the shared default.
- No permission-drift checking yet (blocked on the enforcement build, not a v1 cut).
- No new alerting channel — reuse and de-duplicate against what exists (`workerCanary.js`'s `sendAlerts`), not a second parallel path.
- No LLM-generated fixes or judgments — checks and remediation templates are both fixed code.

## Legal note, flagged not resolved here

The AI-disclosure footer (CODEX 97, resolved "yes" 2026-09-21) may not be optional best-practice — the EU AI Act's transparency obligations for AI systems interacting with people are understood to have started applying August 2026, which is directly relevant given the Estonia entity and EU DPP work already in flight. Worth a real check with counsel before any persona sends externally under its own name — noted here so it doesn't get lost, not resolved in this doc.

---

## Changelog

**2026-09-23 — IAM grant done, real incident found and fixed the same session:** Sean granted the 4 viewer roles to `dev-worker-readonly@title-app-alpha.iam.gserviceaccount.com` himself; "read-only" is now a real IAM fact. The first real scheduled run under that identity immediately threw `PERMISSION_DENIED` — `devWorker.js` had been persisting its own results and sending alerts directly, neither of which a read-only identity can do. Fixed by splitting the module: `devWorker.js` is now purely read-only (computes findings only); a new internal endpoint hosted by `api` (`/v1/internal/dev-worker/record-findings`, shared-secret header, direct Cloud Run URL — the Frontdoor silently strips custom headers) does the writing and alerting. Separately fixed: two capabilityGates.js predicates prove themselves via real writes and can't be safely re-executed under Dev's restricted identity — Dev now checks those two aren't an obvious hardcoded stub (source-shape check) instead of executing them, while the two genuinely read-only predicates are still actually executed. Verified clean via a real Cloud Scheduler trigger, not just a local test.

**2026-09-22, Dev v1 built (same night as the safeguard build, after Ivy per Sean's sequencing):**
- Checks #1 (gate-registry integrity), #2 (Gmail watch health, cross-checked against gate state), #3 (pending-approval age), #5 (workerCanary findings surfaced, explicitly excluded from Dev's own alert trigger to avoid double-paging) — all real, all verified against production Firestore, not mocked.
- #7 (watching Dev itself) added as `checkDevHeartbeat()` inside `workerCanary.js`, not inside Dev — confirmed by a real run that correctly reported `warn` ("Dev has never reported") before Dev's first check, matching the doc's own bootstrapping expectation.
- **Real gap found and fixed while building check #2:** `watchMailbox()`/`stopWatch()` never persisted watch state anywhere — there was no stored answer to "does a watch exist" at all, only the return value of a one-off HTTP call. Added `config/gmailPersonaWatchState` (`active`, `historyId`, `expiration`, `startedAt`, `stoppedAt`, `stoppedReason`), and wired the webhook's existing bypass-detection `stopWatch()` call to record a real reason.
- Registered as a real worker (`digitalWorkers/platform-dev`) with only the fields `useWorkerCatalog.js` actually reads — deliberately did not fabricate RAAS-tier/compliance fields real spine-governed workers carry, since Dev isn't RAAS-governed. No `canvasSpec` yet (doc only conditionally calls for one; deferred, the real interface is meant to be the eventual staff-meeting surface).
- Deferred, matching the doc: #4 (config-existence — moved to deploy-time validation, not built this pass), #6 (permission drift — blocked on the enforcement build), Pub/Sub dead-letter/undelivered-count cross-check mentioned under check #2 (would need a new Cloud Monitoring API dependency; skipped rather than half-built).
- **Access boundary — NOT fully satisfied yet, flagged honestly:** created `dev-worker-readonly@title-app-alpha.iam.gserviceaccount.com` (currently zero roles, inert), but granting it `roles/datastore.viewer` + `logging.viewer` + `monitoring.viewer` + `pubsub.viewer` is an IAM policy-binding change the deploy environment's own safety classifier declined to let an agent do unattended. Dev runs under the default service account until Sean (or a future session with his explicit go-ahead) runs that grant — exact commands are commented directly above `exports.devWorker` in `index.js`. This doc calls the dedicated SA "a hard requirement before Dev ships, not a nice-to-have"; that requirement is open, not met.

**2026-09-22, round 1 red-team → fixed same session:** first draft put the "activated without gates met" check inside Dev (after-the-fact). Fixed: gate moved into code, inside `watchMailbox()` itself via a new registry (`config/capabilityGates.js`). Also found and fixed: the Cloud Scheduler renewal job was a second, ungated activation path calling the same function — paused, then closed by the same gate since both paths share `watchMailbox()`.

**2026-09-22, round 2 red-team → fixed same session:**
- **Continuous enforcement, not just at activation.** A Gmail watch persists up to ~7 days even if renewal is refused, so "decline to renew" doesn't turn anything off. Fixed: `checkGatesStatus()` (non-throwing) is now called (a) inside `gmailPushWebhook.js` on every inbound Pub/Sub push, and (b) inside `gmail.js`'s `sendEmail()` itself for any persona `fromEmail`, before every send — not duplicated per call site, centralized in the one function all sends go through.
- **Pub/Sub message arrival as ground truth.** The Gmail API has no "does a watch currently exist" endpoint. A push message actually arriving is stronger evidence than any stored flag (which a bypass wouldn't update). The webhook now treats "message arrived while gates are failing" as red, and actively calls `stopWatch()` in response — not just declines to process.
- **Tamper detection.** A predicate stubbed to return `true` would be indistinguishable from a real pass. Fixed with two mechanisms: (1) `capabilityGates.js` hashes its own source at check time and compares against `CAPABILITY_GATES_APPROVED_HASH` (an env var, so the approved value lives outside the file being checked) — any mismatch fails every gate regardless of predicate results; (2) `capabilityGates.test.js`, 14 negative-path assertions (each predicate fails closed right now for a real reason; missing/mismatched hash fails closed; unregistered capability fails closed), wired into `firebase.json`'s functions `predeploy` alongside a new grep-based lint rule (`scripts/lint-gmail-watch-callsites.js`) that fails the deploy if `gmail.users.watch()` is ever called anywhere except the one gated function. **Verified live**, same session: both ran and passed as part of the actual deploy, confirmed via `--debug` output, not assumed.
- **Honest caveat on the pinned hash:** the current `CAPABILITY_GATES_APPROVED_HASH` value was computed and set by the same session that wrote the file it approves — real review of it by Sean hasn't happened yet. The mechanism will correctly catch any *future* unreviewed edit; it does not retroactively mean *this* version was independently reviewed.
- **CODEOWNERS already covers this file** (`/functions/` → `@seanlcombs`, blanket), but GitHub branch protection requiring code-owner approval is **not enabled** on `main` — confirmed via `gh api`, not assumed — so CODEOWNERS currently only *requests* review, doesn't *block* merge. Deliberately not flipped on unilaterally: Sean is flying nights for the next several days, and repo-wide branch protection requiring his approval could block unrelated merges while he's unavailable. **Open decision for Sean, not defaulted:** enable it now, or wait until his schedule allows fast turnaround on review requests.

**Still open, not built this session (round 2, not yet actioned):**
- **Metadata-only access is not yet a real control**, same gap as "read-only" was before its own fix — `roles/datastore.viewer` reads whole documents, bodies included. Real enforcement needs either a separate datastore/DB for message bodies with its own IAM, or a narrow internal read endpoint returning only metadata. Deferred until Dev (and its access pattern) is actually being built, not before.
- **Watch-the-watcher, outside app code.** `workerCanary.js` watching Dev doesn't help if the whole functions deployment goes down — both go silent together. Needs a Cloud Monitoring alert on a missing heartbeat metric, which runs independently of this codebase. Deferred until Dev exists and has something to heartbeat.
- **Findings' `reason` strings must never carry untrusted content** (e.g. an email subject line) into whatever narrates them — build from fixed templates filling in only IDs/counts/timestamps. Design rule for whenever Dev is actually built; nothing to enforce yet since Dev doesn't exist as code.
- **Approval-queue single point of failure**, given Sean's LFN flying schedule — needs a decided backup-approver (a real role/access decision, not just adding a name) or a fail-safe default (stale drafts expire unsent, never auto-send on timeout). If drafts expire unsent, decide whether that's logged/surfaced so a real person who never got a reply can be followed up with by hand. **Not decided, flagged for Sean.**
- **Billing lag** (~1 day unless billing export to BigQuery is set up) means "cost anomaly" checks can't be real-time — noted so a future check isn't designed assuming same-day data.
- **Alert-path de-duplication** between Dev's future alerts and `workerCanary.js`'s existing `sendAlerts` — one layer, not two parallel pings for the same incident. Not yet built (Dev doesn't exist yet to alert from).
